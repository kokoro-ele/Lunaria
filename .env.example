# Optional: force a single moon texture URL (HTTPS + CORS required).
# VITE_MOON_TEXTURE_URL=https://your-cdn.example/moon_color_4k.jpg
#
# Default: CDN 2K / 4K / 8K tiers in src/lib/moonTexture.ts, with same-origin
# fallback. 2K boots the app; higher tiers are loaded only when requested.
